const pages=[...document.querySelectorAll('.page')],links=[...document.querySelectorAll('.nav a')],menu=document.querySelector('.nav'),btn=document.querySelector('.menu-btn');function show(){const id=(location.hash||'#inicio').slice(1);pages.forEach(p=>p.classList.toggle('active',p.id===id));links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+id));menu.classList.remove('open');btn.setAttribute('aria-expanded','false');window.scrollTo(0,0)}window.addEventListener('hashchange',show);btn.addEventListener('click',()=>{menu.classList.toggle('open');btn.setAttribute('aria-expanded',menu.classList.contains('open')?'true':'false')});show();